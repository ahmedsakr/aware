
/**
 * Start a new direct message with a related user.
 *
 * @param myUsername The username of the user submitting this request
 * @param theirUsername The target user to start the direct message with
 */
export async function startDirectMessage(myUsername: string, theirUsername: string) {

}